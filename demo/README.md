# 旧静态原型归档说明

本目录不再提供运行入口。原 `index.html`、`app.js` 与 `styles.css` 已在全栈版本通过构建和浏览器验收后移除，避免出现两套并行系统。

当前唯一实现位于项目根目录的 Next.js 全栈工程，按根目录 `README.md` 启动。旧版 UI 规范仅作为设计演进记录保留；移除前的完整文件可从 `backups/pre-fullstack-20260823.zip` 恢复，但该备份不进入 GitHub 或 Coze 导入包。
